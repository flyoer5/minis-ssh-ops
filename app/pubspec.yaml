name: ssh_ai_agent
description: 机枢 — SSH 运维 Agent
publish_to: "none"
version: 1.5.38+122

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.3
  provider: ^6.1.2
  web_socket_channel: ^3.0.1
  flutter_markdown_plus: ^1.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/go/README.txt
