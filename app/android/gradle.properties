org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G
android.useAndroidX=true
android.enableJetifier=true
# Flutter plugin still uses old DSL; avoid AGP 9 newDsl path if host defaults change
android.newDsl=false
