# opsd binary placeholder

`opsd_arm64` is **not** committed (too large). Generate with:

```bash
# from repo root
./app/scripts/prepare_assets.sh
```

CI runs the same script after cross-compiling `GOOS=linux GOARCH=arm64`.
